// Since v0.2, AMLL no longer injects its styles at runtime; the stylesheet must
// be imported explicitly.
import "@applemusic-like-lyrics/core/style.css";

export { LyricPlayer } from "@applemusic-like-lyrics/react";
